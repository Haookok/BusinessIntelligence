server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://1.15.92.20:3306/BIDB?useSSL=false&serverTimezone=UTC
    username: root
    password: noSxw#t?=4wu
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE 